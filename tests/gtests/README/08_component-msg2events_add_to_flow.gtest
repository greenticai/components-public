#SET PACK_DIR=/tmp/test-pack/component-msg2events
#SET FLOW=${PACK_DIR}/flows/main.ygtc
#SET FIXTURES=${WORK_DIR}/fixtures
#SET DEFAULT_ANSWERS=${WORK_DIR}/default.answers.json
#SET SETUP_ANSWERS=${WORK_DIR}/setup.answers.json
#RUN rm -rf ${PACK_DIR}
#RUN mkdir -p /tmp/test-pack
#WRITE ${DEFAULT_ANSWERS} <<<EOF
{}
EOF
#WRITE ${SETUP_ANSWERS} <<<EOF
{
  "default_flow": "alerts",
  "default_event_type": "message.command"
}
EOF
#RUN greentic-pack new --dir ${PACK_DIR} ai.greentic.component-msg2events-gtest
#EXPECT_EXIT 0
#RUN greentic-flow new --flow ${FLOW} --id ai.greentic.component-msg2events.gtest --type messaging --name "component-msg2events gtest" --description "Wizard add-step smoke test" --force
#EXPECT_EXIT 0
#RUN cargo run --quiet -p gtest-fixture-exporter -- --reference repo://local/component-msg2events --out ${FIXTURES} --default-answers ${DEFAULT_ANSWERS} --setup-answers ${SETUP_ANSWERS}
#EXPECT_EXIT 0
#RUN greentic-flow add-step --flow ${FLOW} --node-id msg2events_default --component repo://local/component-msg2events --resolver fixture://${FIXTURES} --wizard-mode default --routing-out
#EXPECT_EXIT 0
#RUN greentic-flow add-step --flow ${FLOW} --node-id msg2events_personalized --component repo://local/component-msg2events --resolver fixture://${FIXTURES} --wizard-mode setup --answers-file ${SETUP_ANSWERS} --routing-out --after msg2events_default
#EXPECT_EXIT 0
#RUN grep -q 'msg2events_default:' ${FLOW}
#EXPECT_EXIT 0
#RUN grep -q 'msg2events_personalized:' ${FLOW}
#EXPECT_EXIT 0
#RUN grep -q 'default_flow: alerts' ${FLOW}
#EXPECT_EXIT 0
#RUN grep -q 'default_event_type: message.command' ${FLOW}
#EXPECT_EXIT 0
#SAVE_ARTIFACT ${DEFAULT_ANSWERS}
#SAVE_ARTIFACT ${SETUP_ANSWERS}
#SAVE_ARTIFACT ${FLOW}
